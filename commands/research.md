---
description: Start a new evidence-first research run.
argument-hint: <research brief, constraints, and desired deliverable>
model: inherit
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/running-evidence-research/SKILL.md` and execute it in `start` mode using `$ARGUMENTS` as the research brief. Create the run artifacts before delegating work. Do not declare completion until the audit passes.
