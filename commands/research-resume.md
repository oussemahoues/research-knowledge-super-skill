---
description: Resume an existing evidence-research run from its last valid checkpoint.
argument-hint: <run-id-or-path> [updated constraints]
model: inherit
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/running-evidence-research/SKILL.md` and execute it in `resume` mode using `$ARGUMENTS`. Validate `run.json`, preserve completed artifacts, and continue only from the recorded legal state.
